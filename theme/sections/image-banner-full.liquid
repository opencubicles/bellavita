{% schema %}
{
  "name": "Responsive Banner",
  "settings": [
    {
      "type": "image_picker",
      "id": "desktop_image",
      "label": "Desktop Banner Image (1920×400)"
    },
    {
      "type": "image_picker",
      "id": "mobile_image",
      "label": "Mobile Banner Image"
    }
  ],
  "presets": [
    {
      "name": "Responsive Banner"
    }
  ]
}
{% endschema %}

<div class="responsive-banner">
  {% if section.settings.desktop_image != blank %}
    <img src="{{ section.settings.desktop_image | image_url: width: 2000 }}"
         alt="Desktop Banner"
         class="banner-image desktop-banner">
  {% endif %}

  {% if section.settings.mobile_image != blank %}
    <img src="{{ section.settings.mobile_image | image_url: width: 1000 }}"
         alt="Mobile Banner"
         class="banner-image mobile-banner">
  {% endif %}
</div>

<style>
.page-title {
    display:none;
}
.responsive-banner {
    padding-top: 243px;
    width: 100%;
    margin: 0 auto;
}

.banner-image {
  width: 100%;
  height: auto;
  display: none;
}

/* Show desktop image */
.desktop-banner {
  display: block;
}

.mobile-banner {
  display: none;
}

/* Switch to mobile image */
@media (max-width: 767px) {
   .responsive-banner {
        padding-top: 170px;
        width: 100%;
        margin: 0 auto;
    }
  .desktop-banner {
    display: none;
  }
  .mobile-banner {
    display: block;
  }
}
<style>
    .meta-coms-container {
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      margin-top: 3rem !important;
    }
    .meta-coms-img-container {
      flex-basis: calc(25% - 10px);
      position: relative;
      overflow: hidden;
      text-align: center;
      background: #f2f2f2;
      padding: 18px 5px;
      border-radius:8px;
    }
    .meta-coms-img-container img {
      display: block;
      margin: auto;
    }
    .meta-coms-img-container p {
      margin-top: 10px;
      margin-bottom: 0;
      text-transform: uppercase;
      color: #262020;
      font-size: 12px;
      line-height: 16px;
      font-weight: 600;
    }
    @media only screen and (max-width: 749px) {
      .meta-coms-img-container {
        /* flex-grow: 1;
        min-width: calc(50% - 10px);
        padding: 15px;
        background: none; */
        width: 23%;
        background: #F2F2F2;
        padding: 7px;
        border-radius:5px;
      }
      .meta-coms-container {
        /* gap: 20px; */
        margin-top: 2.5rem !important;
        display: flex;
        justify-content: space-between;
        width: 100%;
      }
    }
  </style>
</style>
